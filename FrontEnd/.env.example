# Local: Vite proxies /api → backend (see vite.config.ts)
# Production (Vercel Services): use same-origin /api (recommended)
VITE_BACKEND_URL=/api

# Only if API is on a different host:
# VITE_BACKEND_URL=https://your-api.example.com/api
